import promiseNativeConstructor from './promise-native-constructor.js';
import checkCorrectnessOfIteration$1 from './check-correctness-of-iteration.js';
import promiseConstructorDetection from './promise-constructor-detection.js';

var NativePromiseConstructor = promiseNativeConstructor;
var checkCorrectnessOfIteration = checkCorrectnessOfIteration$1;
var FORCED_PROMISE_CONSTRUCTOR = promiseConstructorDetection.CONSTRUCTOR;
var promiseStaticsIncorrectIteration = FORCED_PROMISE_CONSTRUCTOR || !checkCorrectnessOfIteration(function (iterable) {
  NativePromiseConstructor.all(iterable).then(undefined, function () {
    /* empty */
  });
});

export { promiseStaticsIncorrectIteration as default };

import _export from '../internals/export.js';
import arrayBuffer from '../internals/array-buffer.js';
import arrayBufferNative from '../internals/array-buffer-native.js';

var $ = _export;
var ArrayBufferModule = arrayBuffer;
var NATIVE_ARRAY_BUFFER = arrayBufferNative; // `DataView` constructor
// https://tc39.es/ecma262/#sec-dataview-constructor

$({
  global: true,
  constructor: true,
  forced: !NATIVE_ARRAY_BUFFER
}, {
  DataView: ArrayBufferModule.DataView
});

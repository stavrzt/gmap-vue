import '../internals/typed-array-constructor.js';
import { t as typedArrayConstructor } from '../../../_virtual/typed-array-constructor.js';

var createTypedArrayConstructor = typedArrayConstructor.exports; // `Int16Array` constructor
// https://tc39.es/ecma262/#sec-typedarray-objects

createTypedArrayConstructor('Int16', function (init) {
  return function Int16Array(data, byteOffset, length) {
    return init(this, data, byteOffset, length);
  };
});
